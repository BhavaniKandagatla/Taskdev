// Loop through numbers from 1 to 100
for (var i = 1; i <= 100; i++) {
    // Check if the current number is even
    if (i % 2 === 0) {
        // Print the even number
        console.log(i);
    }
}
